# Origin of your deployed Spool web app. It hosts the extension's OAuth
# client metadata (/extension-client-metadata.json) and renders articles.
WXT_SPOOL_ORIGIN=https://spool.example.com

# Optional: public key (base64, no PEM header) to pin the Chrome extension ID.
# SPOOL_EXTENSION_KEY=
